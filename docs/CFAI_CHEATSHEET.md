# CFAI Cheatsheet

> Auto-generated by `adopt.py` on 2026-08-13 — do not edit by hand. Re-run `/adopt-standards` to refresh.

## Installed scripts
- `docs_update.py` — Reviews the app and updates the canonical CFAI docs (CLAUDE.md + docs/). Copied to the project root; git-tracked.
  - Run: `python3 docs_update.py`
  - Self-test: `python3 docs_update.py --self-test`
- `module-audit.py` — AI-assisted, language-agnostic modularity auditor. Copied to the project root; git-tracked.
  - Run: `python3 module-audit.py`
  - Self-test: `python3 module-audit.py --self-test --project .`
- `session-doc-update.ts` — End-of-session living-docs sync (CHANGELOG/STATUS/memory/personas). Adjust the path constants after install, then run `npx tsx session-doc-update.ts`.
  - Run: `npx tsx session-doc-update.ts`
  - Customize first: CHANGELOG_PATH, STATUS_PATH, PERSONAS_DIR
- `standards_report.py` — Generates docs/STANDARDS_ADOPTION_REPORT.md — per-standard currency (deterministic) + optional --grade adherence.
  - Run: `python3 standards_report.py`
  - Self-test: `python3 standards_report.py --self-test --project .`
- `scripts/idea_to_backlog.py` — Idea → Backlog AI workflow config. Tag `@idea_to_backlog.py <note>` in a Claude session (fill the CONFIGURE blocks first).
  - Run: `python3 scripts/idea_to_backlog.py`
  - Customize first: IDEAS_LOG_PATH, BACKLOG_PATH, PRD_PATH, PERSONA_DIRECTORY, AREAS, ID_PREFIXES, PHASES, APP_LAYERS, STABILITY_RISK_FACTORS
- `scripts/idea-to-backlog-workflow.md` — Prose companion to idea_to_backlog.py — the 6-phase workflow the model runs.
  - Run: `scripts/idea-to-backlog-workflow.md`
- `.claude/commands/modularity-audit.md` — Recurring modularity health gate — runs module-audit.py, diffs the grade trend, files the fix-first queue. Invoke with `/modularity-audit`.
  - Run: `/modularity-audit`
- `.claude/commands/completion-audit.md` — Recurring backlog completion & health audit — regenerates the roll-up, runs integrity gates, effort-weights coverage, grades A–F into docs/audits/BACKLOG_AUDIT.md. The recurring form of the backlog-completion-audit reckoner. Invoke with `/completion-audit`.
  - Run: `/completion-audit`
- `remindme.py` — Session-context recall + the --json session facts feed /session-to-backlog reads. Read-only.
  - Run: `python3 remindme.py`
  - Self-test: `python3 remindme.py --self-test`
- `.claude/commands/session-to-backlog.md` — /session-to-backlog — harvest a chat window's work into the backlog.
  - Run: `/session-to-backlog`
- `.claude/commands/backlog-reconcile.md` — /backlog-reconcile — correct backlog drift against the real codebase.
  - Run: `/backlog-reconcile`

## Adopted standards
- EAS Build Profiles (eas.json) → `docs/standards/mobile/eas-json-profiles.md`
- EAS Build Failure Diagnosis & Environment Setup → `docs/standards/mobile/eas-build-diagnostics.md`
- iOS Code Signing → `docs/standards/mobile/ios-signing-reckoner.md`
- OTA Updates Decision (eas update vs eas build) → `docs/standards/mobile/ota-updates-reckoner.md`
- Apple App Store Submission Track → `docs/standards/cicd/apple-app-store-submission-track.md`
- Google Play Submission Track → `docs/standards/cicd/google-play-submission-track.md`
- Store Pre-Submission Checklist (iOS + Android) → `docs/standards/compliance/store-pre-submission-checklist.md`
- iOS-Specific Pre-Submission Checklist → `docs/standards/compliance/ios-store-pre-submission-checklist.md`
- Git Secrets Hygiene & .gitignore → `docs/standards/security/git-secrets-hygiene-reckoner.md`
- GitHub Repository Security Setup & Hardening → `docs/standards/security/github-repo-security-setup-reckoner.md`
- Architecture Principles — Modularity Rubric → `docs/standards/architecture/architecture-principles-reckoner.md`
- Modularity Audit — Function/Module/API Health Scorecard → `docs/standards/architecture/modularity-audit-reckoner.md`
- Idea to Backlog — Rough Note to Documented Backlog Item → `docs/standards/project-init/idea-to-backlog-reckoner.md`
- Claude Code Response Status Footer → `docs/standards/ai/claude-code-status-footer-reckoner.md`
  - End EVERY response with the CFAI status footer (🟢 ALL CLEAR / 🟡 PENDING / 🔴 ACTION NEEDED) separated by a `---`, leading with the state so attention items are scannable. When a response created or updated backlog items, append the six-column session ledger table (Item, Title, Type, Component, Persona, Status) for THAT response only; omit it entirely otherwise — see docs/standards/ai/claude-code-status-footer-reckoner.md.
- Project Documentation Standards → `docs/standards/project-init/project-documentation-standards-reckoner.md`
  - Keep the canonical docs in sync: run `python3 docs_update.py --self-test` before committing doc changes, and `npx tsx session-doc-update.ts` at the end of each session (adjust its path constants first).
- App Review Rejection Playbook — From Rejection to Resubmission → `docs/standards/cicd/app-review-rejection-playbook.md`
  - Never delete an App Store Connect record — it permanently burns the bundle ID. Rename it instead.
- EAS Submit — iOS CLI Push to the App Store → `docs/standards/cicd/eas-submit-ios-cli-reckoner.md`
- Learnings to Proposal — Nugget Promotion Reckoner → `docs/standards/project-init/learnings-to-proposal-reckoner.md`

## Key commands
- Refresh / adopt standards: `/adopt-standards` (or `python3 adopt.py --project . --repo <gold-standards-repo>`)
- Reviews the app and updates the canonical CFAI docs (CLAUDE.md + docs/). Copied to the project root; git-tracked: `python3 docs_update.py`
- AI-assisted, language-agnostic modularity auditor. Copied to the project root; git-tracked: `python3 module-audit.py`
- End-of-session living-docs sync (CHANGELOG/STATUS/memory/personas). Adjust the path constants after install, then run `npx tsx session-doc-update.ts`: `npx tsx session-doc-update.ts`
- Generates docs/STANDARDS_ADOPTION_REPORT.md — per-standard currency (deterministic) + optional --grade adherence: `python3 standards_report.py`
- Idea → Backlog AI workflow config. Tag `@idea_to_backlog.py <note>` in a Claude session (fill the CONFIGURE blocks first): `python3 scripts/idea_to_backlog.py`
- Prose companion to idea_to_backlog.py — the 6-phase workflow the model runs: `scripts/idea-to-backlog-workflow.md`
- Recurring modularity health gate — runs module-audit.py, diffs the grade trend, files the fix-first queue. Invoke with `/modularity-audit`: `/modularity-audit`
- Recurring backlog completion & health audit — regenerates the roll-up, runs integrity gates, effort-weights coverage, grades A–F into docs/audits/BACKLOG_AUDIT.md. The recurring form of the backlog-completion-audit reckoner. Invoke with `/completion-audit`: `/completion-audit`
- Session-context recall + the --json session facts feed /session-to-backlog reads. Read-only: `python3 remindme.py`
- /session-to-backlog — harvest a chat window's work into the backlog: `/session-to-backlog`
- /backlog-reconcile — correct backlog drift against the real codebase: `/backlog-reconcile`
